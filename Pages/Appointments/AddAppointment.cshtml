@page
@model Csharp3_A1.Pages.Appointments.AddAppointmentModel
@{
}
<h2>Add Appointment</h2>

<div>
	<label>User: </label>
	@if (Model.IsPatient)
	{
		<input class="form-control" value="@Model.CurrentUser?.Patient?.Name" readonly />
	}
	@if (Model.IsStaff)
	{
		<input class="form-control" value="@Model.CurrentUser?.Staff?.Name" readonly />
	}
</div>

<form method="post">
	<div class="form-group">
		<label asp-for="Appointment.Reason"></label>
		<input asp-for="Appointment.Reason" class="form-control" />
		<span asp-validation-for="Appointment.Reason" class="text-danger"></span>
	</div>

	<div class="form-group">
		<label asp-for="Appointment.DateOfAppointment"></label>
		<input asp-for="Appointment.DateOfAppointment" type="datetime-local" class="form-control" />
		<span asp-validation-for="Appointment.DateOfAppointment" class="text-danger"></span>
	</div>

	@if (Model.IsPatient)
	{
		<div class="form-group">
			<label asp-for="Appointment.StaffId"></label>
			<select asp-for="Appointment.StaffId" class="form-select" asp-items="Model.SelectStaff"></select>
		</div>
	}
	@if (Model.IsStaff)
	{
		<div class="form-group">
			<label asp-for="Appointment.PatientId"></label>
			<select asp-for="Appointment.PatientId" class="form-select" asp-items="Model.SelectPatients"></select>
		</div>
	}

	<input type="hidden" asp-for="Appointment.PatientId" />
	<input type="hidden" asp-for="Appointment.StaffId" />

	<button type="submit" class="btn btn-primary mt-2">Save</button>
	<a asp-page="/Appointments/Index" class="btn btn-outline-primary mt-2">Cancel</a>
</form>